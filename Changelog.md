# Changelog

## [0.0.1] - 2026 — *versão atual*

### Adicionado
- Geração de estrutura de frontend (React JS e React TS)
- Geração de estrutura de backend (Node JS e Node TS)
- `package.json` global com instalação e execução unificada (`install:all`, `dev`, `build`, `start`)